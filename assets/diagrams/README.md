# Diagrams

Place exported public-safe diagrams here.

The README uses a Mermaid architecture diagram, so this folder is optional unless a static PNG/SVG version is useful for LinkedIn, portfolio pages or PDF applications.
